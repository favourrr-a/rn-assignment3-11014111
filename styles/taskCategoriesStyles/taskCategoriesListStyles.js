import { StyleSheet } from 'react-native';

export const taskCategoriesListStyles = StyleSheet.create({
    taskCategoryListContainer: {
        display: 'flex', 
        flexDirection: 'row',
        height: 180,
        marginTop: 10,
    },
})