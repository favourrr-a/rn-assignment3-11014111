import { StyleSheet } from 'react-native';

export const searchFilterButton = StyleSheet.create({
    searchFilterButtonContainer: {
        position: 'absolute',
        right: 0,
        height: '100%',
        width:'14%',
    },
    searchFilterButtonBackgroundRectangle: {
        justifyContent: 'center',
        backgroundColor: '#F0522F',
        borderRadius: 10,
        height: '100%',
        width: '100%',
        alignItems: 'center',
    }    
})