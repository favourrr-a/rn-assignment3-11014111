import { StyleSheet } from 'react-native';

export const ongoingTasksStyles = StyleSheet.create({
    ongoingTasksContainer: {
        marginTop: 30,
    },
    ongoingTasksTitle: {
        fontSize: 23,
        fontWeight: 'bold',
    }
})