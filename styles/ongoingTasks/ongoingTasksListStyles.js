import { StyleSheet } from 'react-native';

export const ongoingTasksListStyles = StyleSheet.create({
    ongoingTasksContainer: {
        display: 'flex', 
        flexDirection: 'column',
        height: 180,
        marginTop: 10,
    },
})