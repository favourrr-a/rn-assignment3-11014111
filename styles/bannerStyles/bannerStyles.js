import { StyleSheet } from "react-native";

export const bannerStyles = StyleSheet.create({
  bannerContainer: {
    width: '100%',
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
  },
});