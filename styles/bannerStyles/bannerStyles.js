import { StyleSheet } from "react-native";

export const bannerStyles = StyleSheet.create({
    banner_container: {
      width: '100%',
      height: 50,
      backgroundColor: '#F4A698',
      justifyContent: 'center',
    },
  });